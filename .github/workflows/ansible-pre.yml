name: Ansible — Configurar VM PRE

on:
  push:
    branches:
      - develop

jobs:
  ansible-pre:
    name: Configurar VM PRE con Ansible
    runs-on: ubuntu-latest
    environment: pre

    steps:
      - name: Checkout rama develop
        uses: actions/checkout@v4
        with:
          ref: develop

      # Obtiene la IP pública directamente del output de Terraform Cloud
      - name: Obtener IP de VM PRE desde Terraform Cloud
        id: tf_output
        run: |
          WORKSPACE_ID=$(curl -s \
            --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/organizations/${{ vars.TF_ORGANIZATION }}/workspaces/Infraestructura_Pre" \
            | jq -r '.data.id')

          VM_IP=$(curl -s \
            --header "Authorization: Bearer ${{ secrets.TF_API_TOKEN }}" \
            --header "Content-Type: application/vnd.api+json" \
            "https://app.terraform.io/api/v2/workspaces/${WORKSPACE_ID}/current-state-version-outputs" \
            | jq -r '.data[] | select(.attributes.name == "vm_public_ip") | .attributes.value')

          echo "vm_ip=${VM_IP}" >> $GITHUB_OUTPUT
          echo "IP obtenida: ${VM_IP}"

      - name: Instalar Ansible
        run: sudo apt-get update && sudo apt-get install -y ansible

      - name: Configurar clave SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa_tfg
          chmod 600 ~/.ssh/id_rsa_tfg
          ssh-keyscan -H ${{ steps.tf_output.outputs.vm_ip }} >> ~/.ssh/known_hosts

      - name: Crear inventario dinámico
        run: |
          cat > ansible/inventory_ci.ini << EOF2
          [vm_pre]
          ${{ steps.tf_output.outputs.vm_ip }} ansible_user=azureuser ansible_ssh_private_key_file=~/.ssh/id_rsa_tfg ansible_python_interpreter=/usr/bin/python3
          EOF2

      - name: Ejecutar playbook Ansible
        run: |
          ansible-playbook -i ansible/inventory_ci.ini ansible/playbook.yml \
            -e "app_user=azureuser app_dir=/opt/microservicio app_port=8000"
